upstream lifelogger { # name of our web container
    server web:8000;
}

server {
    listen 80;
    server_name localhost;

    location / {
        proxy_pass http://lifelogger/;
    }

    location /static/ {
        alias /home/app/lifelogger/static/; # where our static files are hosted
    }
}