upstream django {
    server django-app:8000;
}

server {
    listen 80;
    server_name db;

    location / {
        proxy_pass http://django;
    }

    location /static/ {
        alias /app/static/;
    }
}
